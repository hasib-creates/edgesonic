Place your TensorFlow Lite model files in this directory.
Example: assets/models/demo_model.tflite
You can also add optional label files alongside the model.
For the Edge Sonic anomaly detector, provide a metadata JSON (e.g. model_metadata.json)
containing the SVDD center vector and thresholds when using custom models.
